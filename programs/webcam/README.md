# webcam

URL paramter is:
```
fit
```

Possible values:
- `default` (or nothing): scale X to width of window; fixed on top; cut off bottom
- `cover`: scale to either width or height of window; fixed in center
- `contain`: scale video with letterboxes or pillarboxes

## Notes
Click top right for webcam-selection (if you have multiple cameras)

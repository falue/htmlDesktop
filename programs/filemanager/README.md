# fileManager
For file/folder structure, this öooks for `folders.json` in `../../workstations/${workstationName}/folders.json`.

## URL parameters
```
darkMode               sets darkMode
view                   "list" or anything else. If list, changes view of current folder to list view.
folderContent           Opened folder of "folders.json"
```

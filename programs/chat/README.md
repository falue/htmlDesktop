# CHAT

```
[
    {
        "message": "This is the text in a message. If is sent to us, length of this determines duration of 'typing indicator'.",
        "data": "if an image, or link, or icon, or so",
        "action": "when clicked; javascript to eval?????? or function?",
        "sender": "name of sender. if it's 'me', its written by the user",
        "delay": "integer - if sender is not 'me', this is the time it takes the sender to answer after last message was sent",
        "date": "string - set any date, to be shown in between messages of not on the same date",
        "sent": "boolean - gets a 'sent' check if sender is 'me'",
        "seen": "boolean - gets a 'seen' check if sender is 'me'",
        "error": "string - gets printed like 'message couldn't be delivered' check if sender is not 'me'"
    },
    {
        "message": "",
        "data": "",
        "action": "",
        "sender": "",
        "delay": "",
        "date": "",
        "sent": "",
        "seen": "",
        "error": ""
    }, ...
]
```

